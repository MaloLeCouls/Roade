# Lance Roade sans connaître Python ni Node : `docker compose up --build`,
# puis ouvrir http://localhost:8000.
services:
  roade:
    build: .
    image: roade
    container_name: roade
    ports:
      - '8000:8000'
    environment:
      # Transmet le mot de passe optionnel depuis l'hôte (cf. PasswordGate).
      # Non définis → portail inactif : `docker compose up` reste ouvert en local.
      - ROADE_PASSWORD
      - ROADE_USER
    volumes:
      # Persiste les projets (fichiers importés, workflows, résultats) hors du
      # conteneur — sinon ils disparaîtraient à chaque recréation.
      - ./projects:/app/projects
    restart: unless-stopped
